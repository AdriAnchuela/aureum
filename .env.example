# Optional: official FRED API key (https://fred.stlouisfed.org/docs/api/api_key.html).
# Phase 1 uses the keyless fredgraph.csv endpoint; the key is only needed if you
# switch to the official API for higher rate limits.
FRED_API_KEY=

# Where the parquet lake and the DuckDB warehouse live (defaults are repo-relative).
AUREUM_LAKE_DIR=
AUREUM_DATA_DIR=
